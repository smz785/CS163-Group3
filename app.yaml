runtime: python310
instance_class: F4_1G

automatic_scaling:
  target_cpu_utilization: 0.90
  max_instances: 1

entrypoint: gunicorn -b :$PORT --timeout 300 --workers 1 app:server

env_variables:
  BUCKET_NAME: 'group-3-bucket'