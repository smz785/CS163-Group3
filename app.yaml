runtime: python310
instance_class: F2 #may need F4

automatic_scaling:
  target_cpu_utilization: 0.90
  max_instances: 1

entrypoint: gunicorn -b :$PORT --timeout 120 app:server

env_variables:
  BUCKET_NAME: 'group-3-bucket'